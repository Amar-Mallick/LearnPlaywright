let isDisplayed = true;
if (isDisplayed) {
    console.log('Displayed working fine');
}

//If-Else ............
let isEnabled = true;
if (isEnabled) {
    console.log('Enabled');
} else {
    console.log('Not Enabled');
}