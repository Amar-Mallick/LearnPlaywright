/**
 * Hoisting : Without creating a variable and assigned value want to get the value of varible.
 * Internally jS engine in JS create a variables and value should be show undefined.
 */
console.log(name);
var name = "Amar";
console.log(name);