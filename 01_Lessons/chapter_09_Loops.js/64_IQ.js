/**Write a while loop that prints numbers from 10 down to 1. After reaching 1, print the string "Blastoff!" to the console. */
let c = 10;
while (c >= 1) {
    console.log(c);
    c--;
} if (c = 1) {
    console.log('Blastoff');
}