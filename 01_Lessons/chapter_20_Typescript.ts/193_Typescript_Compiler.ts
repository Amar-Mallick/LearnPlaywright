/*When you run using "tscfilename.ts" the typescript compiler simply convert typescript code in to Javascript code file.
for run that code you can use command like "nodefilename.js"
*/

let name: string = "Amar";
function add_ts(a: number, b: number): number {
    return (a + b);
}
console.log(add_ts(20, 30));//50
