//coderunner or in terminal  using  command 'ts-node' with filname.ts directly can run ts.Fiile. 
function sname(student: string): string {
    return student;
}
console.log(sname("Amar"));//Amar

function tname(teacher: string): void {
    console.log(teacher);
}
tname("Praveen");//Praveen